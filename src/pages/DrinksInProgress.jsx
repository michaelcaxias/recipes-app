import React from 'react';
// import { useParams } from 'react-router';

export default function DrinksInProgress() {
  // const { id } = useParams();
  return (
    <div>
      bebida in progress
    </div>
  );
}
